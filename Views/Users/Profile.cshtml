@model BlogHost.Data.Models.User

@{
    ViewData["Title"] = "Profile";
}

<style>
.container .img{
    text-align:center;
}
.container .details{
    border-left:3px solid #ded4da;
}
.container .details p{
    font-size:15px;
    font-weight:bold;
}
</style>

<h3 class="text-center">Profile</h3>


<div class="container">
    <div class="row">
        <div class="col-md-6 img">
            @if ((User.IsInRole("admin") == false) && (User.IsInRole("moder") == false))
            {
                <img style='width:300px; height:300px;' src="data:image/jpeg;base64,@(Convert.ToBase64String(@Model.Avatar))" alt="" class="img-rounded" />
            }
        </div>
        <div class="col-md-6 details">
            <blockquote>
                <h5>
                    @Model.Name
                    @Model.SecondName
                </h5>
                <small><cite title="Source Title">@Model.Email <i class="icon-map-marker"></i></cite></small>
            </blockquote>
            <table>
                <tr>
                    <td>Год рождения:</td>
                    
                    <td>@Model.Year</td>
                </tr>
                <tr>
                    <td>Пол:</td>
                    
                    <td>@Model.Gender</td>
                </tr>
                <tr>
                    <td>Дата регистрации:</td>
                    
                    <td>@Model.DateRegistration</td>
                </tr>
                <tr>
                    <td>Род деятельности:</td>
                    <td>@Model.Profession</td>
                </tr>
                <tr>
                </tr>
                <tr>
                    <td>
                        <form asp-action="Delete" asp-route-id="@Model.Id" method="post">
                            <a class="btn btn-sm btn-primary" asp-action="Edit" asp-route-id="@Model.Id">Редактировать профиль</a>
                            <a class="btn btn-sm btn-primary" asp-action="ChangePassword" asp-route-id="@Model.Id">Сменить пароль</a>
                        </form>
                    </td>
                </tr>
            </table>
        </div>
    </div>
</div>


